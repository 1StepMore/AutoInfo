# =============================================================================
# Scenario: collectors-e2e
# Description: E2E validation for all 26 AutoInfo collector handlers.
#   Covers 21 GAP collectors (A2-A29) with real bash CLI scenarios.
#   No-key collectors: real collect + cache JSON structure verification.
#   Key-required collectors: env_required gate for graceful SKIP.
# Tags: collectors, e2e, regression, coverage
# =============================================================================

name: "Collectors E2E — All 26 Handler Validation"
tier: 3
description: >
  Executable bash CLI scenarios for every AutoInfo collector handler.
  Includes Step 0 VALID_SOURCE_TYPES completeness check, real CLI
  collection (--limit 2) with cache JSON assertion for no-key handlers,
  and env_required-gated steps for key-required handlers (NYT/AP/Reuters/
  YouTube/Spotify/Quandl/Unpaywall/CORE/Kaggle).  Covers GAP collectors
  A2 OpenAlex, A3 Semantic Scholar, A4 DBLP, A5 USPTO, A8 Quandl,
  A9 Reuters/AP, A10 NYT, A12 36kr, A13 GitHub/HN, A14 Reddit,
  A15 YouTube, A16 Spotify/Apple Podcasts, A17 Bilibili, A18 GDELT,
  A21 Web, A23 SSRN, A24 HF/Kaggle, A25 Unpaywall/CORE, A29 Apple Podcasts CN.

tags: [collectors, e2e, regression, coverage]
depends_on: [init-project]

variables:
  BASE_DIR: "/tmp/autoinfo-scenario-collectors"

steps:
  # ---------------------------------------------------------------------------
  # Step 0 — VALID_SOURCE_TYPES completeness check
  # ---------------------------------------------------------------------------
  - name: "Step 0: Verify VALID_SOURCE_TYPES completeness (>=25 types)"
    tool: bash
    command: |
      python3 -c "
      from autoinfo.config import VALID_SOURCE_TYPES
      required = ['openalex','dblp','reddit','ssrn','gdelt','huggingface','kaggle','unpaywall','core']
      missing = [r for r in required if r not in VALID_SOURCE_TYPES]
      assert len(VALID_SOURCE_TYPES) >= 25, f'Only {len(VALID_SOURCE_TYPES)} types, expected >=25'
      assert not missing, f'Missing required types: {missing}'
      print(f'VALID_SOURCE_TYPES count={len(VALID_SOURCE_TYPES)}')
      print(f'ALL_REQUIRED_PRESENT=True')
      " 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "ALL_REQUIRED_PRESENT=True"
      stdout_not_contains:
        - "Traceback"
        - "AssertionError"

  # ===========================================================================
  # medical-research domain group
  # Collectors: OpenAlex, Semantic Scholar, DBLP, USPTO, SSRN, GDELT
  # ===========================================================================

  # --- A2: OpenAlex ---
  - name: "A2: OpenAlex collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/openalex && mkdir -p ${BASE_DIR}/openalex
    command: |
      cd ${BASE_DIR}/openalex && autoinfo init --demo medical-research --name "oa-test" 2>&1
      autoinfo collect --domain medical-research --source openalex --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/openalex/collections/medical-research/openalex/*/*.json:!"

  # --- A3: Semantic Scholar ---
  - name: "A3: Semantic Scholar collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/semantic-scholar && mkdir -p ${BASE_DIR}/semantic-scholar
    command: |
      cd ${BASE_DIR}/semantic-scholar && autoinfo init --demo medical-research --name "s2-test" 2>&1
      autoinfo collect --domain medical-research --source semantic-scholar --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/semantic-scholar/collections/medical-research/semantic-scholar/*/*.json:!"

  # --- A4: DBLP ---
  - name: "A4: DBLP collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/dblp && mkdir -p ${BASE_DIR}/dblp
    command: |
      cd ${BASE_DIR}/dblp && autoinfo init --demo medical-research --name "dblp-test" 2>&1
      autoinfo collect --domain medical-research --source dblp --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/dblp/collections/medical-research/dblp/*/*.json:!"

  # --- A5: USPTO ---
  - name: "A5: USPTO collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/uspto && mkdir -p ${BASE_DIR}/uspto
    command: |
      cd ${BASE_DIR}/uspto && autoinfo init --demo medical-research --name "uspto-test" 2>&1
      autoinfo collect --domain medical-research --source uspto --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/uspto/collections/medical-research/uspto/*/*.json:!"

  # --- A23: SSRN (added to medical-research) ---
  - name: "A23: SSRN collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/ssrn && mkdir -p ${BASE_DIR}/ssrn
    command: |
      cd ${BASE_DIR}/ssrn && autoinfo init --demo medical-research --name "ssrn-test" 2>&1
      autoinfo sources add --domain medical-research --name ssrn --type ssrn --url "https://papers.ssrn.com/sol3/papers.cfm" 2>&1
      autoinfo collect --domain medical-research --source ssrn --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/ssrn/collections/medical-research/ssrn/*/*.json:!"

  # --- A18: GDELT (added to medical-research) ---
  - name: "A18: GDELT collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/gdelt && mkdir -p ${BASE_DIR}/gdelt
    command: |
      cd ${BASE_DIR}/gdelt && autoinfo init --demo medical-research --name "gdelt-test" 2>&1
      autoinfo sources add --domain medical-research --name gdelt --type gdelt --url "https://api.gdeltproject.org/api/v2/doc/doc" 2>&1
      autoinfo collect --domain medical-research --source gdelt --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/gdelt/collections/medical-research/gdelt/*/*.json:!"

  # ===========================================================================
  # tech-ai-developer domain group
  # Collectors: GitHub Trending, HackerNews API, Reddit, Bilibili, HuggingFace
  # ===========================================================================

  # --- A13: GitHub Trending ---
  - name: "A13: GitHub Trending collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/github-trending && mkdir -p ${BASE_DIR}/github-trending
    command: |
      cd ${BASE_DIR}/github-trending && autoinfo init --demo tech-ai-developer --name "gh-test" 2>&1
      autoinfo collect --domain tech-ai-developer --source "GitHub Trending" --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/github-trending/collections/tech-ai-developer/*/*/*.json:!"

  # --- A13: HackerNews API ---
  - name: "A13: HackerNews API collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/hackernews && mkdir -p ${BASE_DIR}/hackernews
    command: |
      cd ${BASE_DIR}/hackernews && autoinfo init --demo tech-ai-developer --name "hn-test" 2>&1
      autoinfo collect --domain tech-ai-developer --source "HackerNews API" --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/hackernews/collections/tech-ai-developer/*/*/*.json:!"

  # --- A14: Reddit (config-gated: needs client_id + client_secret in config) ---
  - name: "A14: Reddit collect (env-gated: credentials required)"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/reddit && mkdir -p ${BASE_DIR}/reddit
    command: |
      cd ${BASE_DIR}/reddit && autoinfo init --demo tech-ai-developer --name "reddit-test" 2>&1
      autoinfo collect --domain tech-ai-developer --source Reddit --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
      stdout_not_contains:
        - "Traceback"

  # --- A17: Bilibili ---
  - name: "A17: Bilibili collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/bilibili && mkdir -p ${BASE_DIR}/bilibili
    command: |
      cd ${BASE_DIR}/bilibili && autoinfo init --demo tech-ai-developer --name "bili-test" 2>&1
      autoinfo collect --domain tech-ai-developer --source "Bilibili (B站)" --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/bilibili/collections/tech-ai-developer/*/*/*.json:!"

  # --- A24: HuggingFace (added to tech-ai-developer) ---
  - name: "A24: HuggingFace collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/huggingface && mkdir -p ${BASE_DIR}/huggingface
    command: |
      cd ${BASE_DIR}/huggingface && autoinfo init --demo tech-ai-developer --name "hf-test" 2>&1
      autoinfo sources add --domain tech-ai-developer --name huggingface --type huggingface --url "https://huggingface.co/api/models" 2>&1
      autoinfo collect --domain tech-ai-developer --source huggingface --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/huggingface/collections/tech-ai-developer/huggingface/*/*.json:!"

  # ===========================================================================
  # ai-commercial domain group
  # ===========================================================================

  # --- A12: 36kr ---
  - name: "A12: 36kr collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/36kr && mkdir -p ${BASE_DIR}/36kr
    command: |
      cd ${BASE_DIR}/36kr && autoinfo init --demo ai-commercial --name "36kr-test" 2>&1
      autoinfo collect --domain ai-commercial --source 36kr --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/36kr/collections/ai-commercial/36kr/*/*.json:!"

  # ===========================================================================
  # Standalone / generic handlers
  # ===========================================================================

  # --- A21: Web ---
  - name: "A21: Web collect + cache verify"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/web && mkdir -p ${BASE_DIR}/web
    command: |
      cd ${BASE_DIR}/web && autoinfo init --demo medical-research --name "web-test" 2>&1
      autoinfo sources add --domain medical-research --name web-test --type web --url "https://example.com" 2>&1
      autoinfo collect --domain medical-research --source web-test --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/web/collections/medical-research/web-test/*/*.json:!"

  # --- A16/A29: Apple Podcasts (no-key, free iTunes Search API) ---
  - name: "A16/A29: Apple Podcasts collect + cache verify (EN search)"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/apple-podcasts && mkdir -p ${BASE_DIR}/apple-podcasts
    command: |
      cd ${BASE_DIR}/apple-podcasts && autoinfo init --demo medical-research --name "apod-test" 2>&1
      autoinfo sources add --domain medical-research --name apple-podcasts --type apple_podcasts --url "https://itunes.apple.com/search" 2>&1
      autoinfo collect --domain medical-research --source apple-podcasts --topic "science" --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/apple-podcasts/collections/medical-research/apple-podcasts/*/*.json:!"

  # --- A29: Apple Podcasts CN (Chinese query via iTunes API) ---
  - name: "A29: Apple Podcasts CN collect (ZH query)"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/apple-podcasts-cn && mkdir -p ${BASE_DIR}/apple-podcasts-cn
    command: |
      cd ${BASE_DIR}/apple-podcasts-cn && autoinfo init --demo medical-research --name "apod-cn-test" 2>&1
      autoinfo sources add --domain medical-research --name apple-podcasts-cn --type apple_podcasts --url "https://itunes.apple.com/search" 2>&1
      autoinfo collect --domain medical-research --source apple-podcasts-cn --topic "科技" --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/apple-podcasts-cn/collections/medical-research/apple-podcasts-cn/*/*.json:!"

  # ===========================================================================
  # ENV-GATED collectors (SKIP without API key via env_required)
  # ===========================================================================

  # --- A10: NYT (env-gated: AUTOINFO_NYT_API_KEY) ---
  - name: "A10: NYT collect (env-gated: AUTOINFO_NYT_API_KEY)"
    tool: bash
    env_required:
      - AUTOINFO_NYT_API_KEY
    setup: |
      rm -rf ${BASE_DIR}/nyt && mkdir -p ${BASE_DIR}/nyt
    command: |
      cd ${BASE_DIR}/nyt
      autoinfo init --demo medical-research --name "nyt-test" 2>&1
      autoinfo sources add --domain medical-research --name nyt --type nyt --url "https://api.nytimes.com/svc/search/v2/articlesearch.json" 2>&1
      autoinfo collect --domain medical-research --source nyt --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/nyt/collections/medical-research/nyt/*/*.json:!"

  # --- A9: AP API (env-gated: AUTOINFO_AP_API_KEY) ---
  - name: "A9: AP API collect (env-gated: AUTOINFO_AP_API_KEY)"
    tool: bash
    env_required:
      - AUTOINFO_AP_API_KEY
    setup: |
      rm -rf ${BASE_DIR}/ap-api && mkdir -p ${BASE_DIR}/ap-api
    command: |
      cd ${BASE_DIR}/ap-api
      autoinfo init --demo medical-research --name "ap-test" 2>&1
      autoinfo sources add --domain medical-research --name ap --type ap_api --url "https://api.ap.org/v2/search" 2>&1
      autoinfo collect --domain medical-research --source ap --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/ap-api/collections/medical-research/ap/*/*.json:!"

  # --- A9: Reuters MCP (env-gated: AUTOINFO_REUTERS_API_KEY) ---
  - name: "A9: Reuters MCP collect (env-gated: AUTOINFO_REUTERS_API_KEY)"
    tool: bash
    env_required:
      - AUTOINFO_REUTERS_API_KEY
    setup: |
      rm -rf ${BASE_DIR}/reuters && mkdir -p ${BASE_DIR}/reuters
    command: |
      cd ${BASE_DIR}/reuters
      autoinfo init --demo medical-research --name "reuters-test" 2>&1
      autoinfo sources add --domain medical-research --name reuters --type reuters_mcp --url "https://reuters-mcp.example.com/api" 2>&1
      autoinfo collect --domain medical-research --source reuters --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/reuters/collections/medical-research/reuters/*/*.json:!"

  # --- A15: YouTube (env-gated: AUTOINFO_YOUTUBE_API_KEY) ---
  - name: "A15: YouTube collect (env-gated: AUTOINFO_YOUTUBE_API_KEY)"
    tool: bash
    env_required:
      - AUTOINFO_YOUTUBE_API_KEY
    setup: |
      rm -rf ${BASE_DIR}/youtube && mkdir -p ${BASE_DIR}/youtube
    command: |
      cd ${BASE_DIR}/youtube
      autoinfo init --demo tech-ai-developer --name "yt-test" 2>&1
      autoinfo sources add --domain tech-ai-developer --name youtube-api --type youtube --url "https://www.googleapis.com/youtube/v3/search" 2>&1
      autoinfo collect --domain tech-ai-developer --source youtube-api --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/youtube/collections/tech-ai-developer/youtube-api/*/*.json:!"

  # --- A16: Spotify (env-gated: AUTOINFO_SPOTIFY_CLIENT_ID + SECRET) ---
  - name: "A16: Spotify collect (env-gated: AUTOINFO_SPOTIFY_CLIENT_ID + SECRET)"
    tool: bash
    env_required:
      - AUTOINFO_SPOTIFY_CLIENT_ID
      - AUTOINFO_SPOTIFY_CLIENT_SECRET
    setup: |
      rm -rf ${BASE_DIR}/spotify && mkdir -p ${BASE_DIR}/spotify
    command: |
      cd ${BASE_DIR}/spotify
      autoinfo init --demo tech-ai-developer --name "spotify-test" 2>&1
      autoinfo sources add --domain tech-ai-developer --name spotify-api --type spotify --url "https://api.spotify.com/v1/search" 2>&1
      autoinfo collect --domain tech-ai-developer --source spotify-api --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/spotify/collections/tech-ai-developer/spotify-api/*/*.json:!"

  # --- A8: Quandl (env-gated: AUTOINFO_QUANDL_API_KEY) ---
  - name: "A8: Quandl collect (env-gated: AUTOINFO_QUANDL_API_KEY)"
    tool: bash
    env_required:
      - AUTOINFO_QUANDL_API_KEY
    setup: |
      rm -rf ${BASE_DIR}/quandl && mkdir -p ${BASE_DIR}/quandl
    command: |
      cd ${BASE_DIR}/quandl
      autoinfo init --demo financial-intelligence --name "quandl-test" 2>&1
      autoinfo sources add --domain financial-intelligence --name quandl --type quandl --url "https://data.nasdaq.com/api/v3/datasets" 2>&1
      autoinfo collect --domain financial-intelligence --source quandl --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/quandl/collections/financial-intelligence/quandl/*/*.json:!"

  # --- A25: Unpaywall (env-gated: AUTOINFO_UNPAYWALL_EMAIL) ---
  - name: "A25: Unpaywall collect (env-gated: AUTOINFO_UNPAYWALL_EMAIL)"
    tool: bash
    env_required:
      - AUTOINFO_UNPAYWALL_EMAIL
    setup: |
      rm -rf ${BASE_DIR}/unpaywall && mkdir -p ${BASE_DIR}/unpaywall
    command: |
      cd ${BASE_DIR}/unpaywall
      autoinfo init --demo medical-research --name "unpaywall-test" 2>&1
      autoinfo sources add --domain medical-research --name unpaywall --type unpaywall --url "https://api.unpaywall.org/v2/search" 2>&1
      autoinfo collect --domain medical-research --source unpaywall --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/unpaywall/collections/medical-research/unpaywall/*/*.json:!"

  # --- A25: CORE (env-gated: AUTOINFO_CORE_API_KEY) ---
  - name: "A25: CORE collect (env-gated: AUTOINFO_CORE_API_KEY)"
    tool: bash
    env_required:
      - AUTOINFO_CORE_API_KEY
    setup: |
      rm -rf ${BASE_DIR}/core && mkdir -p ${BASE_DIR}/core
    command: |
      cd ${BASE_DIR}/core
      autoinfo init --demo medical-research --name "core-test" 2>&1
      autoinfo sources add --domain medical-research --name core --type core --url "https://api.core.ac.uk/v3/search/works" 2>&1
      autoinfo collect --domain medical-research --source core --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/core/collections/medical-research/core/*/*.json:!"

  # --- A24: Kaggle (env-gated: KAGGLE_USERNAME + KAGGLE_KEY) ---
  - name: "A24: Kaggle collect (env-gated: KAGGLE_USERNAME + KAGGLE_KEY)"
    tool: bash
    env_required:
      - KAGGLE_USERNAME
      - KAGGLE_KEY
    setup: |
      rm -rf ${BASE_DIR}/kaggle && mkdir -p ${BASE_DIR}/kaggle
    command: |
      cd ${BASE_DIR}/kaggle
      autoinfo init --demo tech-ai-developer --name "kaggle-test" 2>&1
      autoinfo sources add --domain tech-ai-developer --name kaggle --type kaggle --url "https://www.kaggle.com/api/v1/datasets" 2>&1
      autoinfo collect --domain tech-ai-developer --source kaggle --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/kaggle/collections/tech-ai-developer/kaggle/*/*.json:!"

  # --- A15: YouTube RSS (no-key fallback via RSS feed) ---
  - name: "A15: YouTube RSS feed (no-key fallback)"
    tool: bash
    setup: |
      rm -rf ${BASE_DIR}/youtube-rss && mkdir -p ${BASE_DIR}/youtube-rss
    command: |
      cd ${BASE_DIR}/youtube-rss && autoinfo init --demo online-video --name "yt-rss-test" 2>&1
      autoinfo collect --domain online-video --source youtube-mkbhd --limit 2 2>&1
    expected:
      exit_code: 0
      stdout_contains:
        - "AutoInfo initialized"
        - "Collection"
        - "Total:"
      stdout_not_contains:
        - "Traceback"
        - "Error:"
    artifacts:
      - "${BASE_DIR}/youtube-rss/collections/online-video/youtube-mkbhd/*/*.json:!"
