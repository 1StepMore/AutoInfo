# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Project runtime
.autoinfo/
collections/
knowledge/
outputs/

# Validation / delivery artifacts (reproducible, not for git)
validation-deliveries/
deliverables/

# IDE
.idea/
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.*
venv/
.venv/

# OpenCode working files — runtime scratch, never tracked. AGENTS.md
# §Runtime Artifacts vs Source Files declares .omo/ gitignored runtime state
# and docs/dev/workflow-charter.md says .omo/evidence/ is process-only, so
# there is deliberately no re-inclusion here: lessons must be distilled into
# in-repo artifacts (docs/, ADRs, skills), never tracked from .omo/.
.omo/

# Local SQLite database (generated by tests/processing)
autoinfo.db

# Exports
exports/

# Secrets — never commit credential files or token-bearing URLs
.git-credentials
*.pem
*.key
*.p12

# Runtime artifacts
backups/
logs/
reports/
knowledge_graph_export.json
autoinfo.db-wal
autoinfo.db-shm
# Root-level runtime dir only (anchored: an unanchored rule here would also
# match .omo/evidence/validation-runs/ and fight the .omo/ rule above).
/validation-runs/

# Local working dirs (not for git — handoff + generated products)
out/
handoff/
_cleanup_backup_20260831/
autoinfo.db.bak-precrossdom-cleanup-20260831

# Concierge MVP pilot delivery dirs
mvp/

# Demo packages (generated raw+processed bundles — reproducible, not for git)
demo-packages/
